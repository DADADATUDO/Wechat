# 测试环境配置
server:
  port: 0  # 随机端口

spring:
  # 测试数据源配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/winter_im_test?characterEncoding=utf8&useSSL=false&allowMultiQueries=true&serverTimezone=Asia/Shanghai
    username: root
    password: 829829
    hikari:
      auto-commit: true
      connection-test-query: SELECT 1
      maximum-pool-size: 5
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 30000
      max-lifetime: 120000

# MyBatis-Plus配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

# 日志配置
log:
  level:
    root: warn
    sql: debug
    my: debug
  file: logs/test
  maxsize: 2MB